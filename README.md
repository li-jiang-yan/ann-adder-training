# Adder using Single-Layer Perceptron
Constructed a single-layer perceptron in Python as well as spreadsheets to perform addition, training the model using gradient descent. Implemented the single-layer perceptron using TensorFlow Keras as well.
